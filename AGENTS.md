# Marketing Ops Agent Guidance

- This is a private local personal plugin. Never copy credentials, Keychain values, Cookies, storage state, or browser Profiles into the public Algorithm Visualizer repository.
- Use pnpm. Keep the MCP transport local STDIO-only.
- Add or change behavior with red-green tests. Security modules require 100% line, branch, function, and statement coverage.
- Expose only the seven approved high-level MCP tools. Never add browser eval, arbitrary selectors, shell commands, file paths, secret export, or generic HTTP proxy tools.
- Setup may use hidden TTY input, official OAuth/device pages, GitHub CLI auth, or a headed persistent Profile. Main passwords are never accepted.
- T3-B has a fixed typed `gh api` client, read-only GitHub CLI/account/repository health checks, and a nonsecret local activation gate. Healthy authentication alone never enables the adapter; `marketing-ops setup github` must create the activation record, and every write rechecks health before registration.
- T3-C adds strict Release reactions, Issue comments, repository traffic, local status/report/feedback, and known-receipt deletion. Repository traffic is a 14-day repository-level observation and must never be attributed to a campaign. Deleting an owned Release must also clean its reserved Git tag; publication fails if that tag already exists without the expected Release marker.
- The owner-authorized T3-C smoke completed and was cleaned up: its receipt is deleted, the temporary Release/tag are absent, and GitHub remains ready/enabled. Any later write still requires explicit authorization for the matching campaign.
- T3-D1-A adds a fail-closed Weibo boundary around official `@weibo-ai/weibo-cli@0.8.3`. Production may only run fixed `doctor` and available `statuses` catalog reads; strip token/secret environment variables and never expose raw CLI output. The injected text adapter is contract-only. Device OAuth and personal verification are complete, but the 2026-07-14 official plan audit shows Free is a seven-day own-data-only trial with five reads/hour and zero writes. Do not activate a paid plan, consume the short trial without a read-only test need, or describe Free readiness as publish capability. Production publishing stays disabled; any zero-cost browser automation requires a separate reviewed boundary.
- T3-D2 uses fixed official `@atproto/api@0.20.28` for English text-only Bluesky posts. Accept only a public handle plus dedicated App Password from an interactive TTY; keep the App Password in macOS Keychain and the public handle/DID in a strict 0600 activation record. Activation, Keychain handle, and live handle/DID must match before lazy adapter registration. One-time setup completed on 2026-07-14 and the channel is ready/enabled. `bluesky-text@0.2.0` may delete only a published receipt created by this adapter whose AT URI, public URL, and authenticated DID all match. No live post has been created yet; the first matching campaign still requires explicit owner authorization.
- The GitHub process runner must keep `shell: false`, a fixed executable and argument grammar, bounded output/time, and a safe environment allowlist. Never add arbitrary endpoints/arguments, token environment inheritance, `--show-token`, `--include`, or request bodies in argv.
- `publish_campaign` packages must come from the public Algorithm Visualizer renderer. Do not duplicate platform copy/UTM rendering in this plugin.
- `all-or-none` requires an explicit channel set and a complete one-to-one package set. Reject unverifiable `all-authorized` calls before adapter preflight.
- A media type is not an uploaded asset. Reject unresolved media until a validated asset-reference contract exists.
